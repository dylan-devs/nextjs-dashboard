import { lusitana } from '@/app/ui/fonts';

export default function Loading() {
    return (
        <h1 className={`${lusitana.className} font-bold text-purple-800 text-2xl`}>Loading...</h1>
    );
}